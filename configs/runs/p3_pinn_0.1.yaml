inherit: phase2
run_id: p3_pinn_0.1
phase: 3
hypothesis: H6
regime: pushforward   # pinned explicitly (not just inherited from phase2) so this
                       # sweep keeps testing pushforward even if phase2.yaml's
                       # recorded H5 winner ever changes later
M_BACK: 3
N_FWD: 2
LAMBDA_PHYSICS: 0.1
features: [U]   # matches p1_mback3 exactly (M_BACK=3 was only ever validated with
                 # features=[U], never combined with Ut -- see p1_feat_u_ut, which
                 # won H4 at M_BACK=2, not 3) -- also fits comfortably in 96G
                 # (p1_mback3 peaked at 56.3G, same M_BACK+features combination)
